# Example Codex project config.
# Copy to .codex/config.toml in a target repo only if useful.

[project]
name = "codex-fde-hack-kit"

[workflow]
default_loop = "inspect -> smallest useful slice -> plan -> implement -> verify -> summarize diff -> iterate"
