Review `retrospectives/lessons_learned.md` and propose only high-confidence durable rules.

Rules:

- Do not automatically edit `AGENTS.md` unless explicitly approved.
- Reject vague rules.
- Reject rules that duplicate existing instructions.
- Reject rules that only applied to one scenario.
- Prefer small edits.
- Show a diff proposal first.

If a rule is promoted after explicit approval, update `retrospectives/promoted_rules.md`.
