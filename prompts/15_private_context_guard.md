Use private event context without leaking it.

Instructions:

- Treat the private context file as local-only and NDA-sensitive.
- Do not copy private context into repo files, commits, generated docs, demo artifacts, logs, or public summaries.
- Use private context only to improve problem-owner questions, artifact selection, trust boundaries, validation, and demo framing.
- Base the actual implementation on live problem-owner needs and repo evidence.
- If a useful idea depends on private context, restate it in neutral terms before using it.

Return:

```text
Private context used for:
Public-safe framing:
Questions to ask:
Artifact implications:
Trust/guardrail implications:
What not to write down:
```
